import React from "react";
import styled from "styled-components";

function Spinner(props) {
  return (
    <div id="mainSpinner">
      <span class="spinnerInMainSpinner"></span>
    </div>
  );
}

export default Spinner;
